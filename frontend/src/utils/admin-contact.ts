export function adminLink(admin: string) {
    if (admin === "dad") return "https://wa.link/ijor10"
    if (admin === "mom") return "https://wa.link/dcf9ts"
    if (admin === "delight") return "https://wa.link/nub3dq"
}
